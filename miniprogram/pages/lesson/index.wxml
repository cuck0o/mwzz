<van-collapse accordion value="{{ activeName }}" wx:for="{{lessonRecord}}" wx:key="unique" bind:change="onChange" border="{{false}}">

	<van-collapse-item title="{{item.title}}" name="{{item.index}}">
		{{item.info}}
	</van-collapse-item>
</van-collapse>